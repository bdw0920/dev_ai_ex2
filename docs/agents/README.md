# Agentic GitFlow Structure

1. **Project Root**: `repo/` directory contains all work
2. **Changes**: Defined in `repo/changes/CR-*.md` files
3. **Docs**: Documentation lives in `repo/docs/`
4. **Agents**: Agent code organized in `repo/agents/`
5. **Workflow**: Changes > Docs > Implementation > Review